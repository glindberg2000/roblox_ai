function CreateGUI(player)
    if player and player:FindFirstChild("PlayerGui") then
        local playerGui = player.PlayerGui
        -- Proceed with GUI creation
    else
        warn("PlayerGui not found for player")
    end
end 